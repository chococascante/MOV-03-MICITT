import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Separador = () => {
  return (
    <View
      style={{
        backgroundColor: '#000',
        height: 1,
      }}></View>
  );
};

export default Separador;
